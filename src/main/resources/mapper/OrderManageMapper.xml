<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.asghoul.dao.OrderManageMapper">

    <!--   查看我卖出的商品  -->
    <select id="showSoldIitem" parameterType="int" resultType="cn.asghoul.pojo.dto.BoughtSoldItems">
      select items.itemName,items.price,items.pic,sold_items.endTime from items,sold_items where items.itemId=sold_items.itemId
      and clientId=#{clientId}

    </select>

    <!--   查看我买入的商品  -->
    <select id="showBoughtItem" parameterType="int" resultType="cn.asghoul.pojo.dto.BoughtSoldItems">

    select  items.itemName,items.price,items.pic,bought_items.endTime from items,bought_items where items.itemId=sold_items.itemId
      and clientId=#{clientId}

    </select>



</mapper>